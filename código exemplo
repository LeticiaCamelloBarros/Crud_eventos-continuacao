validade_input = "nao"
while validade_input == "nao":
    try:
        numero_ent = int(input())
    except ValueError:
        # ignora essa passada do loop
        continue
    validade_input = "sim"
print(numero_ent*0.5)
